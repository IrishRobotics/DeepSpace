package frc.robot.refs;

import java.util.Map;

public class ArmMapping {
    //public HashMap<Integer, String> armMap = new HashMap<>(540);
    public Map<Integer, String> armMap = Map.of(
        1, "Length:Height",
        2, "Length:Height",
        3, "Length:Height",
        4, "Length:Height"
    );
}